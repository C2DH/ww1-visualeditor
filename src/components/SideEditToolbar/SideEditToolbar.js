import React from 'react'
import './SideEditToolbar.css'

const SideEditToolbar = ({children}) => (
  <div className="SideEditToolbar__container">
     {children}
  </div>
)

export default SideEditToolbar
